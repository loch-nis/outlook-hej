{
  "name": "outlook-hej",
  "version": "1.2.0",
  "private": true,
  "description": "Outlook add-in that writes \"Hej <fornavn>\" at the top of new mails",
  "scripts": {
    "test": "node --test test/*.test.js",
    "build": "node scripts/build.js",
    "validate": "npx --yes office-addin-manifest validate manifest.xml"
  }
}
